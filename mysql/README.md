## how to deploy mysql with docker ?
its a compose file 
you should download ```docker-compose.yml``` file on your server 
after that you should run this command on your server  
```bash
docker-compose up -d
```  
